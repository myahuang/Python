1.my_modbus_serve using queue.py文件采用了队列来处理并发客户端连接，同时最多支持5个连接。while true循环注意要加time.sleep，这样大大降低CPU的负荷。
2.my_modbus_serve.py采用简单的多线程设计，缺陷是开了多线程后，必须所有线程都关闭后，才能重新分配多线程，这样会造成当有一个客户端一直连接，而其他客户端
连接再断开把分配的线程用完后，无法接入新的客户端。
